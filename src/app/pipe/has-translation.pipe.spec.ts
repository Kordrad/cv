import { HasTranslationPipe } from './has-translation.pipe';

describe('HasTranslationPipe', () => {
  it('create an instance', () => {
    const pipe = new HasTranslationPipe();
    expect(pipe).toBeTruthy();
  });
});
